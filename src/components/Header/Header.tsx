import React from "react";
import {Container, Logo} from "./HeaderStyles";

export const Header = ():JSX.Element => {
    return <Container>
        <Logo>
            pipedrive
        </Logo>
    </Container>
}